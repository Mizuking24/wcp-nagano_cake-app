<h2>商品一覧(全<%= @count %>件）</h2>

<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <%= attachment_image_tag item, :image, :fill, 230, 230, fallback: "no_image.jpg" %>
  <% end %>
  <p><%= item.name %></p>
  <p>¥<%= item.price.to_s %></p>
<% end %>
<%= paginate @items %>