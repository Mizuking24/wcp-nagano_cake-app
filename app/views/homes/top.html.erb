<main role="main">

  <section class="jumbotron text-center">
    <div class="home-container">
      <h1 class="jumbotron-heading">Nagano Cake</h1>
      <p class="lead text-muted">
        ようこそ、ながのCAKEへ！</br>
        このサイトは、ケーキ販売のECサイトになります。</br>
        会員でない⽅も商品の閲覧はできますが、</br>
        購⼊には会員登録が必要になります。
      </p>
    </div>
  </section>

  <h2>新着商品</h2>
  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row">

        <% @items.last(4).each do |item| %>
        <div class="col-md-3">
          <div class="card mb-3 shadow-sm">
            <%= link_to item_path(item.id) do %>
              <%= attachment_image_tag item, :image, size: "200x200", fallback: "no_image.jpg", class: "card-img-top" %>
            <% end %>
            <div class="card-body">
              <p class="card-text">
                <%= item.name %>
              </p>
              <p class="card-text">
                ¥<%= item.price %>
              </p>
            </div>
          </div>
        </div>
        <% end %>
        <%= link_to "全ての商品を見る", items_path %>

      </div>
    </div>
  </div>

</main>